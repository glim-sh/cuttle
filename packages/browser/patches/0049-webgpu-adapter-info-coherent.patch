# clark-browser - Copyright 2026 Clark Labs Inc. - MIT
# cuttle-stealth-chromium patch #49 - coherent WebGPU GPUAdapterInfo.
#
# Source: chromium 151.0.7922.137
#   third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
#
# WebGPU exposes adapter identity through GPUAdapter.info and
# GPUDevice.adapterInfo. If WebGL is spoofed to a seed/profile GPU but WebGPU
# reports the host adapter, detectors get a clean cross-API mismatch. This patch
# maps WebGPU vendor/architecture/device/description to the same seed-selected
# GPU pool used by the WebGL patch, and respects explicit
# --fingerprint-gpu-{vendor,renderer} overrides.
diff --git a/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc b/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
--- a/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
+++ b/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
@@ -4,7 +4,13 @@
 
 #include "third_party/blink/renderer/modules/webgpu/gpu_adapter.h"
 
+#include <string>
+#include <string_view>
 #include "services/metrics/public/cpp/ukm_builders.h"
+#include "base/command_line.h"
+#include "base/strings/string_util.h"
+#include "chrome/common/cuttle_fingerprint_switches.h"
+#include "chrome/common/cuttle_seed.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_object_builder.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_gpu_device_descriptor.h"
@@ -50,6 +56,209 @@
   return features;
 }
 
+struct CuttleWebGpuPoolEntry {
+  const char* platform;
+  const char* webgl_vendor;
+  const char* webgl_renderer;
+  const char* vendor;
+  const char* architecture;
+  const char* device;
+  const char* description;
+};
+
+struct CuttleWebGpuInfo {
+  std::string vendor;
+  std::string architecture;
+  std::string device;
+  std::string description;
+};
+
+static constexpr CuttleWebGpuPoolEntry kCuttleWebGpuPool[] = {
+    {"windows",
+     "Google Inc. (Intel)",
+     "ANGLE (Intel, Intel(R) Iris(R) Xe Graphics (0x00009A49) Direct3D11 vs_5_0 ps_5_0, D3D11)",
+     "intel",
+     "xe",
+     "0x9a49",
+     "Intel Iris Xe Graphics"},
+    {"windows",
+     "Google Inc. (NVIDIA)",
+     "ANGLE (NVIDIA, NVIDIA GeForce RTX 3060 (0x00002503) Direct3D11 vs_5_0 ps_5_0, D3D11)",
+     "nvidia",
+     "ampere",
+     "0x2503",
+     "NVIDIA GeForce RTX 3060"},
+    {"windows",
+     "Google Inc. (AMD)",
+     "ANGLE (AMD, AMD Radeon RX 7600 Direct3D11 vs_5_0 ps_5_0, D3D11)",
+     "amd",
+     "rdna3",
+     "",
+     "AMD Radeon RX 7600"},
+    {"macos",
+     "Google Inc. (Apple)",
+     "ANGLE (Apple, ANGLE Metal Renderer: Apple M2, Unspecified Version)",
+     "apple",
+     "apple-m2",
+     "",
+     "Apple M2"},
+    {"macos",
+     "Google Inc. (AMD)",
+     "ANGLE (AMD, ANGLE Metal Renderer: AMD Radeon Pro 5500M, Unspecified Version)",
+     "amd",
+     "rdna",
+     "",
+     "AMD Radeon Pro 5500M"},
+    {"linux",
+     "Google Inc. (Intel)",
+     "ANGLE (Intel, Mesa Intel(R) UHD Graphics 620 (KBL GT2), OpenGL 4.6 (Core Profile) Mesa 23.2.1)",
+     "intel",
+     "kaby-lake",
+     "",
+     "Mesa Intel UHD Graphics 620"},
+    {"linux",
+     "Google Inc. (NVIDIA)",
+     "ANGLE (NVIDIA Corporation, NVIDIA GeForce RTX 3060/PCIe/SSE2, OpenGL 4.6.0 NVIDIA 550.54.14)",
+     "nvidia",
+     "ampere",
+     "",
+     "NVIDIA GeForce RTX 3060"},
+    {"linux",
+     "Google Inc. (AMD)",
+     "ANGLE (AMD, AMD Radeon RX 7600 (radeonsi, navi33, LLVM 17.0.6, DRM 3.57, 6.5.0), OpenGL 4.6)",
+     "amd",
+     "rdna3",
+     "",
+     "AMD Radeon RX 7600"},
+};
+
+bool CuttleHasGpuFingerprint() {
+  auto* cl = base::CommandLine::ForCurrentProcess();
+  return cl->HasSwitch(cuttle::switches::kFingerprint) ||
+         cl->HasSwitch(cuttle::switches::kFingerprintPlatform) ||
+         cl->HasSwitch(cuttle::switches::kFingerprintGpuVendor) ||
+         cl->HasSwitch(cuttle::switches::kFingerprintGpuRenderer);
+}
+
+std::string CuttleFingerprintPlatform() {
+  auto* cl = base::CommandLine::ForCurrentProcess();
+  std::string platform =
+      cl->GetSwitchValueASCII(cuttle::switches::kFingerprintPlatform);
+  if (platform == "macos" || platform == "linux")
+    return platform;
+  return "windows";
+}
+
+const CuttleWebGpuPoolEntry& CuttlePickWebGpu() {
+  const std::string platform = CuttleFingerprintPlatform();
+  size_t candidate_count = 0;
+  for (const auto& entry : kCuttleWebGpuPool) {
+    if (platform == entry.platform)
+      ++candidate_count;
+  }
+  if (candidate_count == 0)
+    return kCuttleWebGpuPool[0];
+
+  size_t pick =
+      static_cast<size_t>(cuttle::seed::Hash("webgl-pool") % candidate_count);
+  for (const auto& entry : kCuttleWebGpuPool) {
+    if (platform != entry.platform)
+      continue;
+    if (pick == 0)
+      return entry;
+    --pick;
+  }
+  return kCuttleWebGpuPool[0];
+}
+
+bool CuttleContains(std::string_view text, std::string_view needle) {
+  return text.find(needle) != std::string_view::npos;
+}
+
+std::string CuttleNormalizeDeviceId(std::string id) {
+  id = base::ToLowerASCII(id);
+  if (!base::StartsWith(id, "0x"))
+    return id;
+  size_t first = id.find_first_not_of('0', 2);
+  if (first == std::string::npos)
+    return "0x0";
+  return "0x" + id.substr(first);
+}
+
+std::string CuttleDeviceIdFromRenderer(const std::string& renderer) {
+  const size_t begin = renderer.find("(0x");
+  if (begin == std::string::npos)
+    return std::string();
+  const size_t end = renderer.find(')', begin);
+  if (end == std::string::npos)
+    return std::string();
+  return CuttleNormalizeDeviceId(renderer.substr(begin + 1, end - begin - 1));
+}
+
+std::string CuttleDescriptionFromRenderer(const std::string& renderer) {
+  size_t start = renderer.find(", ");
+  std::string value =
+      start == std::string::npos ? renderer : renderer.substr(start + 2);
+  static constexpr std::string_view kMetal = "ANGLE Metal Renderer: ";
+  if (base::StartsWith(value, kMetal))
+    value = value.substr(kMetal.size());
+
+  static constexpr std::string_view kMarkers[] = {
+      " (0x", " Direct3D", ", D3D", ", OpenGL", " OpenGL", ", Unspecified"};
+  for (std::string_view marker : kMarkers) {
+    const size_t marker_pos = value.find(marker);
+    if (marker_pos != std::string::npos)
+      value = value.substr(0, marker_pos);
+  }
+  return std::string(base::TrimWhitespaceASCII(value, base::TRIM_ALL));
+}
+
+CuttleWebGpuInfo CuttleInfoFromWebGlStrings(std::string webgl_vendor,
+                                          std::string webgl_renderer) {
+  const std::string joined =
+      base::ToLowerASCII(webgl_vendor + " " + webgl_renderer);
+  CuttleWebGpuInfo info;
+  if (CuttleContains(joined, "nvidia")) {
+    info.vendor = "nvidia";
+    info.architecture = CuttleContains(joined, "rtx 40") ? "ada" : "ampere";
+  } else if (CuttleContains(joined, "amd") || CuttleContains(joined, "radeon")) {
+    info.vendor = "amd";
+    info.architecture = CuttleContains(joined, "rx 7") ? "rdna3" : "rdna";
+  } else if (CuttleContains(joined, "apple")) {
+    info.vendor = "apple";
+    info.architecture = CuttleContains(joined, "m3") ? "apple-m3" :
+        (CuttleContains(joined, "m1") ? "apple-m1" : "apple-m2");
+  } else if (CuttleContains(joined, "intel")) {
+    info.vendor = "intel";
+    info.architecture = CuttleContains(joined, "iris") ? "xe" : "intel";
+  } else {
+    info.vendor = "";
+    info.architecture = "";
+  }
+  info.device = CuttleDeviceIdFromRenderer(webgl_renderer);
+  info.description = CuttleDescriptionFromRenderer(webgl_renderer);
+  return info;
+}
+
+CuttleWebGpuInfo CuttleSelectedWebGpuInfo() {
+  auto* cl = base::CommandLine::ForCurrentProcess();
+  std::string explicit_vendor =
+      cl->GetSwitchValueASCII(cuttle::switches::kFingerprintGpuVendor);
+  std::string explicit_renderer =
+      cl->GetSwitchValueASCII(cuttle::switches::kFingerprintGpuRenderer);
+  if (!explicit_vendor.empty() || !explicit_renderer.empty()) {
+    const auto& fallback = CuttlePickWebGpu();
+    if (explicit_vendor.empty())
+      explicit_vendor = fallback.webgl_vendor;
+    if (explicit_renderer.empty())
+      explicit_renderer = fallback.webgl_renderer;
+    return CuttleInfoFromWebGlStrings(explicit_vendor, explicit_renderer);
+  }
+
+  const auto& picked = CuttlePickWebGpu();
+  return {picked.vendor, picked.architecture, picked.device, picked.description};
+}
+
 }  // anonymous namespace
 
 GPUAdapter::GPUAdapter(
@@ -103,6 +312,15 @@
   }
   description_ = String::FromUtf8(info.device);
   driver_ = String::FromUtf8(info.description);
+  if (CuttleHasGpuFingerprint()) {
+    const CuttleWebGpuInfo clark_info = CuttleSelectedWebGpuInfo();
+    vendor_ = String::FromUtf8(clark_info.vendor);
+    architecture_ = String::FromUtf8(clark_info.architecture);
+    device_ = String::FromUtf8(clark_info.device);
+    description_ = String::FromUtf8(clark_info.description);
+    driver_ = description_;
+  }
+
   if (supportsPropertiesD3D) {
     d3d_shader_model_ = d3dProperties.shaderModel;
   }
@@ -145,7 +363,7 @@
   } else {
     info = MakeGarbageCollected<GPUAdapterInfo>(
         vendor_, architecture_, subgroup_min_size_, subgroup_max_size_,
-        is_fallback_adapter);
+        is_fallback_adapter, device_, description_);
   }
 
   // SAFETY: Required from caller
