apiVersion: v2
name: cuttle
description: Stealth-Chromium CDP farm - one Deployment running `cuttle serve`, reached over kubectl port-forward.
type: application
# Chart version tracks its own shape; appVersion is overridden per-release via
# values.image.tag (pinned to the CLI version by `cuttle up`).
version: 0.1.0
appVersion: "0.3.0"
kubeVersion: ">=1.21.0-0"
home: https://glim.sh
sources:
  - https://github.com/glim-sh/cuttle
keywords:
  - chromium
  - cdp
  - browser
maintainers:
  - name: glim.sh
